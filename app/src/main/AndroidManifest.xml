<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ujuy.ahfizh.belasiswa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.MetodePembayaran"></activity>
        <activity android:name=".activity.Pembayaran" />
        <activity android:name=".activity.RiwayatDonasi" />
        <activity android:name=".activity.DetailDonasi" />
        <activity android:name=".activity.login.login" />
        <activity android:name=".activity.splash.Splashscreen_logo_2" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.splash.Splashscreen" />
        <activity android:name=".activity.register.RegisterActivity" />
        <activity android:name=".activity.login.LoginSign" />
        <activity android:name=".activity.intro.Splashscreen_logo" />
    </application>

</manifest>